import React from 'react';
import { Text, View, Card } from '@core/components';
import { styles } from './card_item.styles';


export const CardItems = (): JSX.Element => {
  return (
    <View style={styles.layout}>

      <Card style={styles.card}>

        
      </Card>
    </View>
  );
};
